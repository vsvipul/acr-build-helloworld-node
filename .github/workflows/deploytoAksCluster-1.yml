on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    
    - uses: actions/checkout@master
    
    - uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Create ACR
      id: create-acr
      run: |
        az acr create -n ddivanshuTest -g ddivanshuTest970f-rg --location "South India" --sku Standard --admin-enabled
        echo "::set-output name=acr_username::`az acr credential show -n ddivanshuTest --query username | xargs`"
        echo "::set-output name=acr_password::`az acr credential show -n ddivanshuTest --query passwords[0].value | xargs`"
        echo "::add-mask::`az acr credential show -n ddivanshuTest --query passwords[0].value | xargs`"

    - uses: azure/docker-login@v1
      with:
        login-server: ddivanshutest.azurecr.io
        username: ${{ steps.create-acr.outputs.acr_username }}
        password: ${{ steps.create-acr.outputs.acr_password }}

    - name: Build and push image to ACR
      id: build-image
      run: |
        docker build "$GITHUB_WORKSPACE/" -f  "Dockerfile" -t ddivanshutest.azurecr.io/diaggarw:${{ github.sha }}
        docker push ddivanshutest.azurecr.io/diaggarw:${{ github.sha }}
    
    - uses: azure/k8s-set-context@v1
      id: login
      with:
         kubeconfig: ${{ secrets.aks_diaggarw_kubeConfig }}
    
    - name: Create namespace
      run: |
        namespacePresent=`kubectl get namespace | grep diaggarweaed | wc -l`
        if [ $namespacePresent -eq 0 ]
        then
            echo `kubectl create namespace diaggarweaed`
        fi

    - uses: azure/k8s-create-secret@v1
      with:
        namespace: diaggarweaed
        container-registry-url: ddivanshutest.azurecr.io
        container-registry-username: ${{ steps.create-acr.outputs.acr_username }}
        container-registry-password: ${{ steps.create-acr.outputs.acr_password }}
        secret-name: diaggarwdockerauth

    - uses: azure/k8s-bake@v1
      with:
        renderEngine: 'helm'
        helmChart: aks-helloworld
        releaseName: 'aks-helm-release'
        helm-version: 'latest' 
        silent: 'false'
        overrideFiles: |
          
        overrides: |
          replicas:2
          image.repository:ddivanshutest.azurecr.io/diaggarw
          image.tag:${{ github.sha }}
          imagePullSecrets:
            -name:diaggarwdockerauth
      id: bake

    - uses: azure/k8s-deploy@v1
      with:
        namespace: diaggarweaed
        manifests: ${{ steps.bake.outputs.manifestsBundle }}
        images: |
          ddivanshutest.azurecr.io/diaggarw:${{ github.sha }}
        imagepullsecrets: |
          diaggarwdockerauth