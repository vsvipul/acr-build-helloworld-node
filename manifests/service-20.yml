apiVersion: v1
kind: Service
metadata:
    name: "diaggarwtest-0a02"
    labels:
        app: "diaggarwtest-0a02"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "diaggarwtest-0a02"