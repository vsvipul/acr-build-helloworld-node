apiVersion : apps/v1beta1
kind: Deployment
metadata:
  name: "diaggarw-3943"
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: "diaggarw-3943"
    spec:
      containers:
        - name: "diaggarw-3943"
          image: "anraghuncr.azurecr.io/diaggarw"
          ports:
          - containerPort: 80