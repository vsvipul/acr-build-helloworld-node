apiVersion: v1
kind: Service
metadata:
    name: "diaggarw-3943"
    labels:
        app: "diaggarw-3943"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "diaggarw-3943"