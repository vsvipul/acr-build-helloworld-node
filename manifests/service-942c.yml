apiVersion: v1
kind: Service
metadata:
    name: "diaggarw-ad2f"
    labels:
        app: "diaggarw-ad2f"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "diaggarw-ad2f"