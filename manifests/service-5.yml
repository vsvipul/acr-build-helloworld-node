apiVersion: v1
kind: Service
metadata:
    name: "diaggarwaks-d3cb"
    labels:
        app: "diaggarwaks-d3cb"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "diaggarwaks-d3cb"