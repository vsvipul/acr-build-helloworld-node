apiVersion: v1
kind: Service
metadata:
    name: "diaggarw-e29c"
    labels:
        app: "diaggarw-e29c"
spec:
    type: LoadBalancer
    ports:
    - port: 
      targetPort: 
      protocol: TCP
      name: http
    selector:
        app: "diaggarw-e29c"