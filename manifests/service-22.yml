apiVersion: v1
kind: Service
metadata:
    name: "diaggarwtest-a649"
    labels:
        app: "diaggarwtest-a649"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "diaggarwtest-a649"