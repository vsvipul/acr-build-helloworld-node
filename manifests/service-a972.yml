apiVersion: v1
kind: Service
metadata:
    name: "diaggarw-2d20"
    labels:
        app: "diaggarw-2d20"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "diaggarw-2d20"