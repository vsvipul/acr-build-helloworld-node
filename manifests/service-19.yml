apiVersion: v1
kind: Service
metadata:
    name: "diaggarwtest-162c"
    labels:
        app: "diaggarwtest-162c"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "diaggarwtest-162c"