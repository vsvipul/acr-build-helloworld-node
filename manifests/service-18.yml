apiVersion: v1
kind: Service
metadata:
    name: "diaggarwtest-289a"
    labels:
        app: "diaggarwtest-289a"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "diaggarwtest-289a"