apiVersion: v1
kind: Service
metadata:
    name: "diaggarw-0485"
    labels:
        app: "diaggarw-0485"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "diaggarw-0485"