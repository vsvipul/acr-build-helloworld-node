apiVersion: v1
kind: Service
metadata:
    name: "diaggarw-dc94"
    labels:
        app: "diaggarw-dc94"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "diaggarw-dc94"