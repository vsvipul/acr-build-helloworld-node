apiVersion: v1
kind: Service
metadata:
    name: "diaggarwtest-e09c"
    labels:
        app: "diaggarwtest-e09c"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "diaggarwtest-e09c"