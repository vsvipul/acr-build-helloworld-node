apiVersion: v1
kind: Service
metadata:
    name: "diaggarwtest-c0d1"
    labels:
        app: "diaggarwtest-c0d1"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "diaggarwtest-c0d1"