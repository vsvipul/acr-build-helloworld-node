apiVersion: v1
kind: Service
metadata:
    name: "diaggarw-a945"
    labels:
        app: "diaggarw-a945"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "diaggarw-a945"