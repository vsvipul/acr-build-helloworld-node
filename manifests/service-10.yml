apiVersion: v1
kind: Service
metadata:
    name: "diaggarwaks-3ddc"
    labels:
        app: "diaggarwaks-3ddc"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "diaggarwaks-3ddc"